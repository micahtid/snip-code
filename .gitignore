# dependencies
node_modules/

# build output
dist/
.tsbuild/
*.tsbuildinfo

# secrets: byok keys live in chrome.storage.local, never in the repo
.env
.env.*
!.env.example

# editor / os
.DS_Store
Thumbs.db
*.log

# generated fixture artifacts (references + snip outputs); the fixture html is tracked
tests/fixtures/.out/
tests/fixtures/tile.png

# generated benchmark log: appended one row per minimize-loop run, never read back
tests/minimize-scores.jsonl

# planning docs and task notes: working notes, never committed
*-PLAN*.md
Tasks/
